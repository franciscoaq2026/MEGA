# Dependências do backend para o deploy no Vercel (Python Serverless Function).
# Espelha backend/requirements.txt + o cliente do Turso (libSQL) usado em produção.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9
libsql>=0.1
